function square(num) {
    return num * num;   
}
console.log(square(2)); // 4

const subtract = (a, b) => {
    return a - b;
};
console.log(subtract(2, 1)); // 1

const sum = (a, b) => a + b;
console.log(sum(2, 3)); // 5


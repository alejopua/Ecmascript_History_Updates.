let name = 'Pedro';
let surname = 'Ramirez';
let epicPhrase = name + ' ' + surname + ' is a good person';
//console.log(epicPhrase);

// Template literals (ES6)
let epicPhrase2 = `${name} ${surname} is a good person`;
//console.log(epicPhrase2);

// Multiline
let lorem = 'Esto es un string \n' + 'Esto es otra linea';

let lorem2 = `Esto es un string
Esto es otra linea`;

console.log(lorem);
console.log(lorem2);